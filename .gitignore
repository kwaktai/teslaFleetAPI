node_modules/
.env
.env.*
!.env.example
*.pem
*.key

# 런타임 비밀 — 토큰, API 키, 차량 데이터
**/tokens.json
**/*-tokens.json
**/api-key.txt

# data/ 폴더 자체는 저장소에 남겨 둡니다.
# 구버전 Docker 데몬은 바인드 마운트 대상 폴더를 자동 생성하지 않아,
# 폴더가 없으면 컨테이너가 기동하지 못합니다.
data/*
!data/.gitkeep
